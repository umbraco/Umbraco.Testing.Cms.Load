# Locust configuration for local development.
# Usage:
#   cd loadtests && locust --host https://<app-service-url>
# Then open http://localhost:8089 to configure and start the test.

locustfile = locustfile.py
